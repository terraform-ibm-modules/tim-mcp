[project]
name = "tim-mcp"
version = "0.1.0"
description = "Default template for PDM package"
dependencies = ["fastmcp>=2.12.3", "asyncio>=4.0.0"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "Apache 2.0"}


[tool.pdm]
distribution = false
